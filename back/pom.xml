<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>org.example</groupId>
    <artifactId>HelloJakarta</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>war</packaging>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <dependencies>
        <!-- 11.0.0, no 10.0.0: Jakarta Data (CrudRepository) es parte de la plataforma
             a partir de Jakarta EE 11, no existe en la 10 que usaba GlassFish 7. Por eso
             este proyecto ahora se despliega en GlassFish 8 (JDK 21+), no en GlassFish 7. -->
        <dependency>
            <groupId>jakarta.platform</groupId>
            <artifactId>jakarta.jakartaee-api</artifactId>
            <version>11.0.0</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>1.18.46</version>
            <scope>provided</scope>
        </dependency>
        <!-- Declarado explicito porque el IDE no lo estaba resolviendo via transitiva
             de jakarta.jakartaee-api (mvn si lo resolvia solo, pero mejor no depender
             de eso). Igual que las otras dos: provided, GlassFish 8 trae la implementacion. -->
        <dependency>
            <groupId>jakarta.data</groupId>
            <artifactId>jakarta.data-api</artifactId>
            <version>1.0.1</version>
            <scope>provided</scope>
        </dependency>
        <!-- MapStruct: genera la implementacion del Mapper en tiempo de compilacion a
             partir de una interfaz (nosotros ya no escribimos ProductoMapper/FacturaMapper
             a mano), mismo espiritu que Jakarta Data con el Repository. Esta si va
             empaquetada en el WAR (compile, no provided): a diferencia de las APIs de
             arriba, el codigo generado lo necesita en tiempo de ejecucion, GlassFish no
             lo trae puesto. -->
        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct</artifactId>
            <version>1.6.3</version>
        </dependency>
    </dependencies>

    <build>
        <finalName>HelloJakarta-variante</finalName>
        <plugins>
            <plugin>
                <!-- Corre npm install + npm run build ANTES de empaquetar el WAR, para que
                     "mvn package" por si solo genere el frontend y lo deje listo en
                     src/main/webapp/. Ya no hace falta correr npm a mano aparte. -->
                <groupId>com.github.eirslett</groupId>
                <artifactId>frontend-maven-plugin</artifactId>
                <version>2.0.1</version>
                <configuration>
                    <workingDirectory>../frontend</workingDirectory>
                </configuration>
                <executions>
                    <execution>
                        <id>install node and npm</id>
                        <goals>
                            <goal>install-node-and-npm</goal>
                        </goals>
                        <configuration>
                            <nodeVersion>v20.20.2</nodeVersion>
                        </configuration>
                    </execution>
                    <execution>
                        <id>npm install</id>
                        <goals>
                            <goal>npm</goal>
                        </goals>
                        <configuration>
                            <arguments>install</arguments>
                        </configuration>
                    </execution>
                    <execution>
                        <id>npm run build</id>
                        <goals>
                            <goal>npm</goal>
                        </goals>
                        <configuration>
                            <arguments>run build</arguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>3.4.0</version>
                <configuration>
                    <failOnMissingWebXml>false</failOnMissingWebXml>
                </configuration>
            </plugin>
            <!-- Declarado explicito (antes no hacia falta un maven-compiler-plugin propio,
                 javac encontraba solo el procesador de anotaciones de Lombok en el
                 classpath). En cuanto se declara annotationProcessorPaths para agregar
                 MapStruct, HAY que listar Lombok tambien aqui: si no, declarar la lista
                 desactiva el escaneo automatico y Lombok deja de correr silenciosamente.
                 lombok-mapstruct-binding es el puente que hace que MapStruct SI vea los
                 getters/setters que Lombok genera (si no, MapStruct los procesa antes de
                 que Lombok los haya generado, y no encuentra nada). -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>1.18.46</version>
                        </path>
                        <path>
                            <groupId>org.mapstruct</groupId>
                            <artifactId>mapstruct-processor</artifactId>
                            <version>1.6.3</version>
                        </path>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok-mapstruct-binding</artifactId>
                            <version>0.2.0</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>